{
    "name": "@dakiya/schemas",
    "version": "1.0.0",
    "type": "module",
    "types": "./dist/index.d.ts",
    "scripts": {
        "generate": "npx tsx scripts/compiler.ts",
        "clean": "rm -rf dist src/validator.js",
        "build": "npm run clean && npm run generate && tsc"
    }
}
